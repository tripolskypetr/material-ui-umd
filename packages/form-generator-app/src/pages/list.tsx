namespace app {

  const {
    Typography,
    makeStyles,
  } = material.core;

  const {
    Fragment
  } = React;

  const useStyles = makeStyles({

  });

  export namespace pages {

    export const List = ({}) => {

      return (
        <Fragment>
          <Typography variant="h6">
            List component sample
          </Typography>
        </Fragment>
      );
    };

  } // namespace pages

} // namespace app
