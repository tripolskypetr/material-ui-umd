namespace app {

  const {
    Typography,
    createStyles,
  } = material.core;

  const {
    Fragment
  } = React;

  const useStyles = createStyles({

  });

  export namespace pages {

    export const List = ({}) => {

      return (
        <Fragment>
          <Typography variant="h6">
            List component sample
          </Typography>
        </Fragment>
      );
    };

  } // namespace pages

} // namespace app
