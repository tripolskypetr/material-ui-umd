namespace form {

  export namespace utils {

    export const isNullOrUndefined = (obj) => {
      return typeof obj === "undefined" || obj === null;
    };

  } // namespace utils

} // namespace form
